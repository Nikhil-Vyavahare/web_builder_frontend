[phases.install]
cmds = ["npm install"]

[phases.build]
dependsOn = ["install"]
cmds = ["ng build web_Builder --configuration production"]

[phases.setup]
cmds = [
  "mkdir -p /app/browser",
  "cp -r dist/web_Builder/* /app/browser/"
]

[start]
cmd = "caddy run --config /app/Caddyfile"
